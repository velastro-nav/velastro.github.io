# Velastro Navigation Infrastructure 

## Getting Started natively
1. Ensure Node.js and npm are installed.
2. Run `npm install`
3. Run `npm run dev`
4. Open [http://localhost:3000](http://localhost:3000)

## Getting Started via Docker
```bash
docker-compose up --build
```
